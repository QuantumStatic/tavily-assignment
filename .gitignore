# Secrets — NEVER commit real keys
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local cache / DB
*.db
*.sqlite
*.sqlite3
# SQLite WAL-mode sidecar files (don't match *.db — they end in -wal/-shm/-journal)
*.db-wal
*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm
.cache/

# Runtime structured-log output — anchored so it does NOT match the
# `src/vendor_dd/logs/` source package (a bare `logs/` did, which silently
# dropped that package from the built wheel).
/logs/
backend/logs/
*.log

# Node / frontend
node_modules/
dist/
.vite/
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# OS / editor
.DS_Store
.idea/
.vscode/

# Assignment scaffold — do not ship in the deliverable repo
starter_agent.py
2606_Tavily_FDE_TakeHomeAssignment.md
.worktrees/

# superpowers brainstorming artifacts
.superpowers/
