"""Thin surfaces over the engine.

Planned modules:
  cli.py         — Typer CLI (engineer / debug surface)
  api.py         — FastAPI app + SSE streaming (web frontend backend)
  mcp_server.py  — MCP server exposing check_vendor() as a tool for other agents
"""
